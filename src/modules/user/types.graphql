type User {
    name: String!
    email: String!
    id: ID!
    role: String
    password: String!
} 

type Result {
  message: String
  status: Int
  data: String
}

type Alltrainees{
  message: String
  totalCount: Int
  traineeCount: Int
  record: [Details]
}

type Details{
  name: String
  _id: String
  role: String
  email: String
  password: String
  createdAt: String
}